SELECT
  sum(c0) OVER w,
  sum(c1) OVER w,
  sum(c2) OVER w,
  sum(c3) OVER w,
  sum(c4) OVER w,
  sum(c5) OVER w,
  sum(c6) OVER w,
  sum(c7) OVER w,
  sum(c8) OVER w,
  sum(c9) OVER w,
  sum(c10) OVER w,
  sum(c11) OVER w,
  sum(c12) OVER w,
  sum(c13) OVER w,
  sum(c14) OVER w,
  sum(c15) OVER w,
  sum(c16) OVER w,
  sum(c17) OVER w,
  sum(c18) OVER w,
  sum(c19) OVER w,
  sum(c20) OVER w,
  sum(c21) OVER w,
  sum(c22) OVER w,
  sum(c23) OVER w,
  sum(c24) OVER w,
  sum(c25) OVER w,
  sum(c26) OVER w,
  sum(c27) OVER w,
  sum(c28) OVER w,
  sum(c29) OVER w,
  sum(c30) OVER w,
  sum(c31) OVER w,
  sum(c32) OVER w,
  sum(c33) OVER w,
  sum(c34) OVER w,
  sum(c35) OVER w,
  sum(c36) OVER w,
  sum(c37) OVER w,
  sum(c38) OVER w,
  sum(c39) OVER w
FROM t1 
WINDOW w AS
  (PARTITION BY t1.id ORDER BY t1.`time` ROWS BETWEEN 10 PRECEDING AND CURRENT ROW)
