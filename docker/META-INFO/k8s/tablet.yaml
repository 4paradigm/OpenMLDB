apiVersion: apps/v1beta2
kind: DaemonSet
metadata:
  name: tablet
  namespace: {{.namespace}}
  labels:
    rtidb: tablet
spec:
  selector: 
    matchLabels:
      rtidb: tablet
  template:
    metadata:
      labels:
        rtidb: tablet
    spec:
      hostNetwork: true
      containers:
      - env:
        - name: zk_cluster
          value: {{.zk_cluster}}
        - name: zk_root_path
          value: "/rtidb"
        - name: port
          value: {{.tablet_port}}
        - name: metric_port
          value: {{.metric_port}}
        - name: MY_NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        image: {{.registry}}/{{.image}}
        name: rtidb
        imagePullPolicy: Always
        volumeMounts:
        - mountPath: /root/db/
          name: db
        - mountPath: /root/recycle/
          name: recycle
      imagePullSecrets:
      - name: docker-secret
      securityContext: {}
      volumes:
      - name: db
        hostPath:
          path: {{.hostpath}}/rtidb/db
          type: DirectoryOrCreate
      - name: recycle
        hostPath:
          path: {{.hostpath}}/rtidb/recycle
          type: DirectoryOrCreate
      - name: logs
        hostPath:
          path: {{.hostpath}}/rtidb/tablet_logs
          type: DirectoryOrCreate
      securityContext:
        runAsUser: 0
      nodeSelector:
        prophet.4paradigm.com/rtidb-tablet: "true"
